# 签名证书（泄露 = 应用可被他人冒名更新）
*.keystore
!android/app/debug.keystore

# 构建产物
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/

# 发布构建产物（体积大且每次打包都会变，仓库只留源码与文档）
deliverables/android/

# 依赖
node_modules/

# 系统 / 编辑器
.DS_Store
.history/
*.log

# TS 增量构建信息
*.tsbuildinfo
